{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch file",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${file}",
      "args": ["create-database, --database-name", "example"],
      "envFile": "${workspaceFolder}/.vscode/.env"
    }
  ]
}
